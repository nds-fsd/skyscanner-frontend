import React from "react";
import logo from "../../files/logo.png"

const Header = () => {
    return (
        <div>
            <img src={logo} alt="Skyreader" />
            <p>Header</p>
        </div>
    );
};

export default Header;